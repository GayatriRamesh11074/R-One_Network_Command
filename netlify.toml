[build]
  command = "npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Player results are stored in Netlify Blobs (see db/blobs-store.ts), so no
# external database is required.
