[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# Player results currently use the Cloudflare D1 API route. Replace that API
# with a Netlify Function backed by Netlify Blobs or an external database
# before deploying this configuration to production.
